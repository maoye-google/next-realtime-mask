# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# MongoDB
MONGO_URI=mongodb://mongo:27017/realtime_mask_db

# Google Cloud Vertex AI (Path to service account key JSON file within the container)
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-service-account.json

# Segment Anything Model Endpoint (if using a separate service, as per plan)
SEGMENT_ANYTHING_ENDPOINT=http://segment-anything:8001/segment